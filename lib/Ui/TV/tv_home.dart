import 'package:flutter/material.dart';

class Tv_Home extends StatelessWidget {
  const Tv_Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       backgroundColor: Colors.black,
    );
  }
}